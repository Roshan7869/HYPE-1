# Page 32 — 404 Not Found (404.html)

## Purpose
Error page for missing routes —friendly, on-brand, and helpful. Gets users back on track quickly instead of a dead end.

## URL
Any unmatched route

## Layout Structure

```
┌─────────────────────────────────────────────────┐
│  NAVBAR (full)                                    │
├─────────────────────────────────────────────────┤
│  404 BODY (bg: --cream)                          │
│  ┌─────────────────────────────────────────────┐│
│  │                                              ││
│  │           404                                ││
│  │     (large, ink, decorative)                 ││
│  │                                              ││
│  │     Page Not Found                           ││
│  │     The page you're looking for doesn't      ││
│  │     exist or has been moved.                 ││
│  │                                              ││
│  │  [Browse Products]  [Go Home]                ││
│  │                                              ││
│  │  ── or try searching ──                      ││
│  │                                              ││
│  │  [___________________________] 🔍             ││
│  │                                              ││
│  │  Popular Pages:                               ││
│  │  Shop · Auctions · Sell With Us · Help       ││
│  │                                              ││
│  └─────────────────────────────────────────────┘│
├─────────────────────────────────────────────────┤
│  FOOTER                                          │
└─────────────────────────────────────────────────┘
```

## Section-by-Section Specs

### 1. Centered Content

| Element | Specs |
|---|---|
| Layout | Centered, `max-width: 560px`, `margin: 0 auto`, `padding: 120px 24px 80px` |
| Text-align | Center |

### 2. 404 Number

| Element | Specs |
|---|---|
| Text | "404" |
| Font | Hanken Grotesk, `font-size: 160px`, `font-weight: 700` |
| Color | `var(--ink)` |
| Line-height | `0.9` |
| Opacity | `0.08` (watermark effect, large but subtle) |
| Position | Centered, overlaps with title |
| Margin-bottom | `-40px` (overlaps) |

### 3. Title

| Element | Specs |
|---|---|
| Text | "Page Not Found" |
| Font | `font-size: 32px`, `font-weight: 700`, `color: var(--ink)` |
| Margin-bottom | `12px` |

### 4. Description

| Element | Specs |
|---|---|
| Text | "The page you're looking for doesn't exist or has been moved. Let's get you back on track." |
| Font | `font-size: 16px`, `color: var(--stone)`, `line-height: 1.6` |
| Max-width | `400px`, centered |
| Margin-bottom | `32px` |

### 5. CTA Buttons

| Element | Specs |
|---|---|
| Layout | Flex row, `gap: 16px`, `justify-content: center` |
| Primary | `.btn-primary`, "Browse Products", `height: 48px`, `padding: 0 32px` |
| Primary href | `/shop` |
| Secondary | `.btn-secondary`, "Go Home", `height: 48px`, `padding: 0 32px` |
| Secondary href | `/` |

### 6. Search Divider

| Element | Specs |
|---|---|
| Layout | Flex row, `align-items: center`, `gap: 16px`, `margin: 40px 0 24px` |
| Line | `flex: 1`, `height: 1px`, `background: #eae8e4` |
| Text | "or try searching" |
| Text font | `font-size: 13px`, `color: var(--stone)`, `text-transform: uppercase`, `letter-spacing: 1px` |

### 7. Search Bar

| Element | Specs |
|---|---|
| Width | `100%` or `480px` max |
| Height | `48px` |
| Border-radius | `24px` (pill shape) |
| Border | `1px solid #e0e0e0` |
| Padding | `0 20px 0 48px` |
| Placeholder | "Search for sneakers, brands, or categories..." |
| Icon | Lucide `search`, `18px`, `color: var(--stone)`, absolute left `16px` |
| Focus | `border-color: var(--ink)`, `box-shadow: 0 0 0 3px rgba(26,24,21,0.08)` |
| Action | Enter redirects to `/shop?q={query}` |

### 8. Popular Pages

| Element | Specs |
|---|---|
| Margin-top | `40px` |
| Label | "Popular Pages" |
| Label font | `font-size: 13px`, `color: var(--stone)`, `text-transform: uppercase`, `letter-spacing: 1px` |
| Links | Flex row, `gap: 16px`, `justify-content: center`, `margin-top: 12px` |
| Link style | `font-size: 15px`, `font-weight: 600`, `color: var(--ink)` |
| Link hover | Underline, `color: var(--ink)` |
| Separator | `·` between links, `color: var(--stone)` |
| Links | Shop → `/shop` · Auctions → `/shop?filter=auction` · Sell With Us → `/sell` · Help → `/faq` |

### 9. Animation (Optional)

| Element | Specs |
|---|---|
| 404 number | Fade in + slight bounce on load, `animation: fadeIn 500ms ease-out` |
| Content | Fade in, `animation: slideUp 400ms ease-out 200ms both` |

```css
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 0.08; transform: translateY(0); }
}
@keyframes slideUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
```

## Data Model

```javascript
// No data model needed — this is a static error page
const notFound = {
  searchQuery: ""  // bound to search input
};
```

## Responsive

| Breakpoint | Behavior |
|---|---|
| > 600px | Full centered layout, buttons side by side |
| ≤ 600px | 404 number 120px, buttons stack vertically, search full width |
| ≤ 400px | 404 number 80px, popular pages wrap |

## Lovable Prompt

> Build the HYPE 404 page. Full navbar. Cream bg, centered content max 560px, 120px top padding. "404" as oversized watermark: Hanken Grotesk 160px bold, ink color at 0.08 opacity, line-height 0.9, -40px bottom margin overlapping title. "Page Not Found" 32px bold ink. Description 16px stone line-height 1.6 max 400px centered. Two CTA buttons centered with 16px gap: "Browse Products" primary 48px linking /shop, "Go Home" secondary linking /. Divider: horizontal line + "or try searching" uppercase 13px stone letter-spacing + horizontal line. Search bar: 48px height 24px pill radius, search icon left, "Search for sneakers, brands, or categories..." placeholder, focus ink border + ring, enter redirects to /shop?q=. "Popular Pages" label 13px uppercase stone, links row: Shop · Auctions · Sell With Us · Help, 15px bold ink with hover underline. Animations: 404 fades in with bounce, content slides up 200ms delay. Mobile: 404 120px, stacked buttons, full-width search. Colors: --ink, --stone, --cream.